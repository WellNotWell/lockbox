# Copy to .env and change the values: cp .env.example .env
# Credentials for the local PostgreSQL and MinIO containers, neither is exposed to the internet.
# Set them before the first start: both are written into the data volumes when they are created.

DB_PASSWORD=lockbox
STORAGE_ACCESS_KEY=lockbox
STORAGE_SECRET_KEY=lockbox-secret
